# Scoreboard

All 56 ideas, scored by their reviewer. Each score runs from 1 to 5. Feasibility asks: can we build this on Cloudflare today? Reliability asks: can a crash or two users at once lose data? Correctness asks: does the proof do what the idea says? Read [how-to-read.md](./how-to-read.md) for what the verdicts mean.

The table is sorted by verdict, best first. Click **plain** for the beginner explainer.

| Verdict | Count |
|---|---|
| lands | 1 |
| lands with caveats | 33 |
| risky | 21 |
| does not land | 1 |

| # | Tier | Idea | Verdict | Feasibility | Reliability | Correctness | Effort | Read |
|---|---|---|---|---|---|---|---|---|
| 53 | foundation | The /info/refs?service= entrypoint and pkt-line codec | lands | 5 | 5 | 4 | days | [plain](../plain/info-refs-endpoint.md) · [proof](../proofs/info-refs-endpoint.md) · [review](../reviews/info-refs-endpoint.md) |
| 1 | foundation | One Durable Object per repo as the ref authority | lands with caveats | 4 | 3 | 3 | weeks | [plain](../plain/repo-do-ref-authority.md) · [proof](../proofs/repo-do-ref-authority.md) · [review](../reviews/repo-do-ref-authority.md) |
| 2 | foundation | Refs in DO SQLite, objects in R2 | lands with caveats | 4 | 2 | 3 | weeks | [plain](../plain/refs-sqlite-objects-r2.md) · [proof](../proofs/refs-sqlite-objects-r2.md) · [review](../reviews/refs-sqlite-objects-r2.md) |
| 4 | foundation | Packfile parsing in a Worker with a streaming inflater | lands with caveats | 4 | 3 | 3 | weeks | [plain](../plain/streaming-pack-parser.md) · [proof](../proofs/streaming-pack-parser.md) · [review](../reviews/streaming-pack-parser.md) |
| 5 | foundation | Content-addressed R2 keys | lands with caveats | 4 | 4 | 4 | days | [plain](../plain/content-addressed-r2-keys.md) · [proof](../proofs/content-addressed-r2-keys.md) · [review](../reviews/content-addressed-r2-keys.md) |
| 6 | foundation | Two-phase push | lands with caveats | 4 | 3 | 3 | weeks | [plain](../plain/two-phase-push.md) · [proof](../proofs/two-phase-push.md) · [review](../reviews/two-phase-push.md) |
| 7 | foundation | Precomputed pack slices for clone | lands with caveats | 4 | 3 | 3 | weeks | [plain](../plain/precomputed-clone-pack.md) · [proof](../proofs/precomputed-clone-pack.md) · [review](../reviews/precomputed-clone-pack.md) |
| 8 | foundation | Delta bases pinned per repo | lands with caveats | 4 | 4 | 3 | weeks | [plain](../plain/pinned-delta-bases.md) · [proof](../proofs/pinned-delta-bases.md) · [review](../reviews/pinned-delta-bases.md) |
| 11 | foundation | Git LFS natively via presigned R2 URLs | lands with caveats | 4 | 3 | 4 | days | [plain](../plain/native-lfs.md) · [proof](../proofs/native-lfs.md) · [review](../reviews/native-lfs.md) |
| 12 | foundation | Bundle-URI support | lands with caveats | 4 | 3 | 3 | days | [plain](../plain/bundle-uri.md) · [proof](../proofs/bundle-uri.md) · [review](../reviews/bundle-uri.md) |
| 13 | edge | Refs replicated to every region via KV and DO location hints | lands with caveats | 4 | 3 | 3 | days | [plain](../plain/replicated-refs-edge.md) · [proof](../proofs/replicated-refs-edge.md) · [review](../reviews/replicated-refs-edge.md) |
| 14 | edge | Push-triggered CI as a DO alarm chain | lands with caveats | 4 | 3 | 3 | weeks | [plain](../plain/alarm-chain-ci.md) · [proof](../proofs/alarm-chain-ci.md) · [review](../reviews/alarm-chain-ci.md) |
| 15 | edge | Live fetch over hibernating WebSockets | lands with caveats | 4 | 3 | 2 | weeks | [plain](../plain/live-fetch-websocket.md) · [proof](../proofs/live-fetch-websocket.md) · [review](../reviews/live-fetch-websocket.md) |
| 19 | edge | Signed refs by default with append-only DO reflog | lands with caveats | 4 | 2 | 3 | weeks | [plain](../plain/signed-reflog.md) · [proof](../proofs/signed-reflog.md) · [review](../reviews/signed-reflog.md) |
| 20 | edge | Time-travel refs | lands with caveats | 4 | 4 | 4 | days | [plain](../plain/time-travel-refs.md) · [proof](../proofs/time-travel-refs.md) · [review](../reviews/time-travel-refs.md) |
| 21 | edge | Snapshots via R2 object versioning of ref state | lands with caveats | 4 | 2 | 3 | days | [plain](../plain/r2-versioned-snapshots.md) · [proof](../proofs/r2-versioned-snapshots.md) · [review](../reviews/r2-versioned-snapshots.md) |
| 26 | edge | Search index built on push (D1 FTS / Vectorize) | lands with caveats | 4 | 3 | 3 | days | [plain](../plain/search-index-on-push.md) · [proof](../proofs/search-index-on-push.md) · [review](../reviews/search-index-on-push.md) |
| 28 | edge | Rate-limited, token-scoped remote URLs | lands with caveats | 4 | 3 | 3 | days | [plain](../plain/scoped-token-remotes.md) · [proof](../proofs/scoped-token-remotes.md) · [review](../reviews/scoped-token-remotes.md) |
| 29 | edge | Push from a sibling workspace DO over RPC, no HTTP | lands with caveats | 4 | 3 | 3 | weeks | [plain](../plain/tui-rpc-push.md) · [proof](../proofs/tui-rpc-push.md) · [review](../reviews/tui-rpc-push.md) |
| 30 | wild | Ephemeral repos with a self-destruct alarm | lands with caveats | 4 | 2 | 3 | days | [plain](../plain/ephemeral-repos.md) · [proof](../proofs/ephemeral-repos.md) · [review](../reviews/ephemeral-repos.md) |
| 32 | wild | Agent-native protocol v2 commands (search, explain-diff, suggest-merge) | lands with caveats | 4 | 3 | 3 | weeks | [plain](../plain/agent-native-commands.md) · [proof](../proofs/agent-native-commands.md) · [review](../reviews/agent-native-commands.md) |
| 34 | wild | Commit-as-event-stream | lands with caveats | 4 | 3 | 3 | days | [plain](../plain/commit-event-stream.md) · [proof](../proofs/commit-event-stream.md) · [review](../reviews/commit-event-stream.md) |
| 38 | wild | Deduplication across all repos in one content-addressed bucket | lands with caveats | 4 | 3 | 3 | weeks | [plain](../plain/global-dedup.md) · [proof](../proofs/global-dedup.md) · [review](../reviews/global-dedup.md) |
| 39 | wild | Merkle inclusion proofs on fetch | lands with caveats | 4 | 5 | 3 | weeks | [plain](../plain/merkle-proofs.md) · [proof](../proofs/merkle-proofs.md) · [review](../reviews/merkle-proofs.md) |
| 41 | wild | Branch previews deployed as Workers on push | lands with caveats | 3 | 3 | 3 | weeks | [plain](../plain/branch-preview-workers.md) · [proof](../proofs/branch-preview-workers.md) · [review](../reviews/branch-preview-workers.md) |
| 43 | wild | Bisect on the server with parallel test Workers | lands with caveats | 4 | 2 | 3 | weeks | [plain](../plain/server-side-bisect.md) · [proof](../proofs/server-side-bisect.md) · [review](../reviews/server-side-bisect.md) |
| 44 | wild | Ref leases | lands with caveats | 5 | 4 | 4 | days | [plain](../plain/ref-leases.md) · [proof](../proofs/ref-leases.md) · [review](../reviews/ref-leases.md) |
| 45 | wild | Commit graph in Vectorize for semantic git log | lands with caveats | 4 | 3 | 2 | weeks | [plain](../plain/vectorized-commit-graph.md) · [proof](../proofs/vectorized-commit-graph.md) · [review](../reviews/vectorized-commit-graph.md) |
| 46 | wild | Time-boxed history with cold-storage checkpoints | lands with caveats | 3 | 3 | 3 | weeks | [plain](../plain/time-boxed-history.md) · [proof](../proofs/time-boxed-history.md) · [review](../reviews/time-boxed-history.md) |
| 47 | wild | Pull-request review data as git objects under refs/reviews | lands with caveats | 4 | 3 | 3 | weeks | [plain](../plain/reviews-as-refs.md) · [proof](../proofs/reviews-as-refs.md) · [review](../reviews/reviews-as-refs.md) |
| 49 | wild | GitHub-compatible webhook payloads | lands with caveats | 4 | 3 | 3 | days | [plain](../plain/github-webhook-compat.md) · [proof](../proofs/github-webhook-compat.md) · [review](../reviews/github-webhook-compat.md) |
| 50 | wild | Storage tiering by heat | lands with caveats | 4 | 3 | 3 | weeks | [plain](../plain/storage-tiering.md) · [proof](../proofs/storage-tiering.md) · [review](../reviews/storage-tiering.md) |
| 54 | foundation | Auth and multi-tenancy: owner/repo routing to DO ids | lands with caveats | 5 | 4 | 4 | days | [plain](../plain/auth-and-multitenancy.md) · [proof](../proofs/auth-and-multitenancy.md) · [review](../reviews/auth-and-multitenancy.md) |
| 56 | edge | Want/have negotiation with a commit-graph in SQLite | lands with caveats | 4 | 3 | 3 | weeks | [plain](../plain/want-have-negotiation.md) · [proof](../proofs/want-have-negotiation.md) · [review](../reviews/want-have-negotiation.md) |
| 3 | foundation | Speak git protocol v2 only, translate v0 at the edge | risky | 4 | 3 | 2 | weeks | [plain](../plain/protocol-v2-only.md) · [proof](../proofs/protocol-v2-only.md) · [review](../reviews/protocol-v2-only.md) |
| 9 | foundation | Tiny in-DO object cache with alarm-driven eviction | risky | 4 | 4 | 2 | days | [plain](../plain/in-do-object-cache.md) · [proof](../proofs/in-do-object-cache.md) · [review](../reviews/in-do-object-cache.md) |
| 10 | foundation | Shallow and partial clone as first-class filters | risky | 3 | 4 | 3 | weeks | [plain](../plain/partial-clone-filters.md) · [proof](../proofs/partial-clone-filters.md) · [review](../reviews/partial-clone-filters.md) |
| 16 | edge | Branch-level Durable Objects for monorepos | risky | 4 | 3 | 2 | weeks | [plain](../plain/branch-level-dos.md) · [proof](../proofs/branch-level-dos.md) · [review](../reviews/branch-level-dos.md) |
| 17 | edge | Server-side three-way merge in the Worker | risky | 4 | 3 | 2 | weeks | [plain](../plain/server-side-merge.md) · [proof](../proofs/server-side-merge.md) · [review](../reviews/server-side-merge.md) |
| 18 | edge | Server-side rebase and squash as protocol v2 extensions | risky | 4 | 2 | 2 | weeks | [plain](../plain/server-side-rebase.md) · [proof](../proofs/server-side-rebase.md) · [review](../reviews/server-side-rebase.md) |
| 22 | edge | Copy-on-write forks | risky | 4 | 2 | 3 | weeks | [plain](../plain/cow-forks.md) · [proof](../proofs/cow-forks.md) · [review](../reviews/cow-forks.md) |
| 23 | edge | Pre/post-receive hooks as Workers via service bindings | risky | 4 | 2 | 3 | weeks | [plain](../plain/hooks-as-workers.md) · [proof](../proofs/hooks-as-workers.md) · [review](../reviews/hooks-as-workers.md) |
| 24 | edge | Per-blob presigned direct upload for giant pushes | risky | 4 | 2 | 3 | weeks | [plain](../plain/presigned-direct-upload.md) · [proof](../proofs/presigned-direct-upload.md) · [review](../reviews/presigned-direct-upload.md) |
| 25 | edge | Wasm git core (gitoxide/libgit2) for delta resolution and merge | risky | 4 | 3 | 3 | weeks | [plain](../plain/wasm-git-core.md) · [proof](../proofs/wasm-git-core.md) · [review](../reviews/wasm-git-core.md) |
| 27 | edge | Diff API served with R2 range reads | risky | 4 | 3 | 2 | weeks | [plain](../plain/diff-api-range-reads.md) · [proof](../proofs/diff-api-range-reads.md) · [review](../reviews/diff-api-range-reads.md) |
| 33 | wild | Semantic diffs via tree-sitter in Wasm | risky | 3 | 3 | 2 | weeks | [plain](../plain/semantic-diffs.md) · [proof](../proofs/semantic-diffs.md) · [review](../reviews/semantic-diffs.md) |
| 35 | wild | Speculative packs | risky | 3 | 4 | 2 | weeks | [plain](../plain/speculative-packs.md) · [proof](../proofs/speculative-packs.md) · [review](../reviews/speculative-packs.md) |
| 36 | wild | Federated remotes via DO-to-DO gossip | risky | 3 | 2 | 3 | weeks | [plain](../plain/federated-gossip.md) · [proof](../proofs/federated-gossip.md) · [review](../reviews/federated-gossip.md) |
| 37 | wild | Encrypted-at-rest with client-held keys | risky | 4 | 2 | 3 | weeks | [plain](../plain/client-key-encryption.md) · [proof](../proofs/client-key-encryption.md) · [review](../reviews/client-key-encryption.md) |
| 40 | wild | Zero-clone execution: repo as a virtual filesystem inside an agent DO | risky | 4 | 2 | 3 | weeks | [plain](../plain/zero-clone-vfs.md) · [proof](../proofs/zero-clone-vfs.md) · [review](../reviews/zero-clone-vfs.md) |
| 42 | wild | Git as a database driver (ActiveRecord / JS ORM adapter) | risky | 4 | 2 | 2 | weeks | [plain](../plain/git-as-db-driver.md) · [proof](../proofs/git-as-db-driver.md) · [review](../reviews/git-as-db-driver.md) |
| 48 | wild | Cross-repo atomic pushes | risky | 3 | 2 | 2 | weeks | [plain](../plain/cross-repo-atomic-push.md) · [proof](../proofs/cross-repo-atomic-push.md) · [review](../reviews/cross-repo-atomic-push.md) |
| 51 | wild | Blame that knows which agent wrote each line | risky | 3 | 3 | 2 | weeks | [plain](../plain/agent-blame.md) · [proof](../proofs/agent-blame.md) · [review](../reviews/agent-blame.md) |
| 52 | wild | Offline-first browser client with OPFS and the same Wasm core | risky | 3 | 3 | 2 | months | [plain](../plain/offline-browser-client.md) · [proof](../proofs/offline-browser-client.md) · [review](../reviews/offline-browser-client.md) |
| 55 | edge | GC and repack as a DO alarm | risky | 3 | 2 | 2 | weeks | [plain](../plain/gc-and-repack-alarm.md) · [proof](../proofs/gc-and-repack-alarm.md) · [review](../reviews/gc-and-repack-alarm.md) |
| 31 | wild | Multi-writer CRDT branches | does not land | 3 | 2 | 2 | weeks | [plain](../plain/crdt-branches.md) · [proof](../proofs/crdt-branches.md) · [review](../reviews/crdt-branches.md) |

## Second pass

14 ideas were rewritten in Rust against [CONTRACTS.md](../CONTRACTS.md) and reviewed again. The table shows first-pass and second-pass scores side by side. A plus sign means the score went up.

| # | Idea | Verdict, first | Verdict, second | Feasibility | Reliability | Correctness | Still open from first pass | New blockers | Read |
|---|---|---|---|---|---|---|---|---|---|
| 1 | One Durable Object per repo as the ref authority | lands with caveats | lands with caveats | 4 → 4 | 3 → 4 (+1) | 3 → 4 (+1) | 0 | 3 | [plain](../plain/repo-do-ref-authority.md) · [proof v2](../proofs-v2/repo-do-ref-authority.md) · [review v2](../reviews-v2/repo-do-ref-authority.md) |
| 2 | Refs in DO SQLite, objects in R2 | lands with caveats | lands with caveats | 4 → 4 | 2 → 4 (+2) | 3 → 4 (+1) | 2 | 2 | [plain](../plain/refs-sqlite-objects-r2.md) · [proof v2](../proofs-v2/refs-sqlite-objects-r2.md) · [review v2](../reviews-v2/refs-sqlite-objects-r2.md) |
| 3 | Speak git protocol v2 only, translate v0 at the edge | risky | lands with caveats | 4 → 4 | 3 → 4 (+1) | 2 → 4 (+2) | 1 | 1 | [plain](../plain/protocol-v2-only.md) · [proof v2](../proofs-v2/protocol-v2-only.md) · [review v2](../reviews-v2/protocol-v2-only.md) |
| 4 | Packfile parsing in a Worker with a streaming inflater | lands with caveats | lands with caveats | 4 → 4 | 3 → 3 | 3 → 4 (+1) | 0 | 2 | [plain](../plain/streaming-pack-parser.md) · [proof v2](../proofs-v2/streaming-pack-parser.md) · [review v2](../reviews-v2/streaming-pack-parser.md) |
| 5 | Content-addressed R2 keys | lands with caveats | lands with caveats | 4 → 5 (+1) | 4 → 4 | 4 → 3 (-1) | 2 | 3 | [plain](../plain/content-addressed-r2-keys.md) · [proof v2](../proofs-v2/content-addressed-r2-keys.md) · [review v2](../reviews-v2/content-addressed-r2-keys.md) |
| 6 | Two-phase push | lands with caveats | lands with caveats | 4 → 4 | 3 → 4 (+1) | 3 → 4 (+1) | 1 | 3 | [plain](../plain/two-phase-push.md) · [proof v2](../proofs-v2/two-phase-push.md) · [review v2](../reviews-v2/two-phase-push.md) |
| 7 | Precomputed pack slices for clone | lands with caveats | risky | 4 → 4 | 3 → 3 | 3 → 4 (+1) | 2 | 2 | [plain](../plain/precomputed-clone-pack.md) · [proof v2](../proofs-v2/precomputed-clone-pack.md) · [review v2](../reviews-v2/precomputed-clone-pack.md) |
| 8 | Delta bases pinned per repo | lands with caveats | lands with caveats | 4 → 4 | 4 → 3 (-1) | 3 → 4 (+1) | 0 | 2 | [plain](../plain/pinned-delta-bases.md) · [proof v2](../proofs-v2/pinned-delta-bases.md) · [review v2](../reviews-v2/pinned-delta-bases.md) |
| 9 | Tiny in-DO object cache with alarm-driven eviction | risky | risky | 4 → 4 | 4 → 3 (-1) | 2 → 4 (+2) | 1 | 2 | [plain](../plain/in-do-object-cache.md) · [proof v2](../proofs-v2/in-do-object-cache.md) · [review v2](../reviews-v2/in-do-object-cache.md) |
| 10 | Shallow and partial clone as first-class filters | risky | risky | 3 → 4 (+1) | 4 → 4 | 3 → 3 | 0 | 2 | [plain](../plain/partial-clone-filters.md) · [proof v2](../proofs-v2/partial-clone-filters.md) · [review v2](../reviews-v2/partial-clone-filters.md) |
| 11 | Git LFS natively via presigned R2 URLs | lands with caveats | lands with caveats | 4 → 4 | 3 → 4 (+1) | 4 → 4 | 1 | 3 | [plain](../plain/native-lfs.md) · [proof v2](../proofs-v2/native-lfs.md) · [review v2](../reviews-v2/native-lfs.md) |
| 12 | Bundle-URI support | lands with caveats | risky | 4 → 4 | 3 → 3 | 3 → 4 (+1) | 2 | 2 | [plain](../plain/bundle-uri.md) · [proof v2](../proofs-v2/bundle-uri.md) · [review v2](../reviews-v2/bundle-uri.md) |
| 53 | The /info/refs?service= entrypoint and pkt-line codec | lands | lands with caveats | 5 → 4 (-1) | 5 → 5 | 4 → 4 | 1 | 1 | [plain](../plain/info-refs-endpoint.md) · [proof v2](../proofs-v2/info-refs-endpoint.md) · [review v2](../reviews-v2/info-refs-endpoint.md) |
| 54 | Auth and multi-tenancy: owner/repo routing to DO ids | lands with caveats | lands with caveats | 5 → 5 | 4 → 4 | 4 → 4 | 3 | 2 | [plain](../plain/auth-and-multitenancy.md) · [proof v2](../proofs-v2/auth-and-multitenancy.md) · [review v2](../reviews-v2/auth-and-multitenancy.md) |

Average across these 14 ideas: feasibility 4.1 → 4.1, reliability 3.4 → 3.7, correctness 3.1 → 3.9.

## Each idea in one breath

The plain description of each idea, followed by its verdict. For the reasons, open the plain explainer.

**#1 One Durable Object per repo as the ref authority.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. A branch is a named line of commits, like a bookmark that moves forward as you save. Verdict: lands with caveats. [Read more](../plain/repo-do-ref-authority.md).

**#2 Refs in DO SQLite, objects in R2.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. A branch is a named line of commits, like a bookmark that moves forward as you save. Verdict: lands with caveats. [Read more](../plain/refs-sqlite-objects-r2.md).

**#3 Speak git protocol v2 only, translate v0 at the edge.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. A branch is a named line of commits, like a bookmark that moves forward as you save. Verdict: risky. [Read more](../plain/protocol-v2-only.md).

**#4 Packfile parsing in a Worker with a streaming inflater.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. A branch is a named line of commits, like a bookmark that moves forward as you save. Verdict: lands with caveats. [Read more](../plain/streaming-pack-parser.md).

**#5 Content-addressed R2 keys.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. A branch is a named line of commits, like a bookmark that moves forward as you save. Verdict: lands with caveats. [Read more](../plain/content-addressed-r2-keys.md).

**#6 Two-phase push.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. A branch is a named line of commits, like a bookmark that moves forward as you save. Verdict: lands with caveats. [Read more](../plain/two-phase-push.md).

**#7 Precomputed pack slices for clone.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. A branch is a named line of commits, like a bookmark that moves forward as you save. Verdict: lands with caveats. [Read more](../plain/precomputed-clone-pack.md).

**#8 Delta bases pinned per repo.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. When one person uploads new work, other people usually download that work soon after. This idea keeps the newest items from each upload close at hand on the server. A download soon after an upload then waits less on the big file store. Verdict: lands with caveats. [Read more](../plain/pinned-delta-bases.md).

**#9 Tiny in-DO object cache with alarm-driven eviction.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. This idea gives each repo a small cache of the items that are read most often. A read that hits the cache never goes to the big file store. A background job cleans the cache on a schedule. Verdict: risky. [Read more](../plain/in-do-object-cache.md).

**#10 Shallow and partial clone as first-class filters.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A partial clone downloads the history but leaves the file contents on the server. A shallow clone downloads only the newest few saved versions. This idea makes both kinds of download a normal part of the server. The server marks the needed items in a small database, then copies them out of big bundles in the file store with a few large reads. Verdict: risky. [Read more](../plain/partial-clone-filters.md).

**#11 Git LFS natively via presigned R2 URLs.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. Git LFS, or Large File Storage, is a git add-on for big files such as images and videos. LFS keeps the big file outside the repo and stores a small pointer in the repo instead. This idea makes the server speak the LFS batch API and hand each client a signed web address for the big file store. The client moves the bytes itself, so the big bytes never pass through the server. Verdict: lands with caveats. [Read more](../plain/native-lfs.md).

**#12 Bundle-URI support.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A git bundle is one file that holds a large part of a repo's history, plus a short list of names at the top. This idea tells the client where to download a ready-made bundle from a fast content network. The client loads the bulk of the history from that file. Then the client asks the server only for what changed since the bundle was made. Verdict: lands with caveats. [Read more](../plain/bundle-uri.md).

**#13 Refs replicated to every region via KV and DO location hints.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. Every download from a repo begins by asking the server for its list of named lines of work. This idea answers that question from a copy of the list stored close to the user. Only one program, in one place, is allowed to change the list. Verdict: lands with caveats. [Read more](../plain/replicated-refs-edge.md).

**#14 Push-triggered CI as a DO alarm chain.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. CI, or continuous integration, is a set of automatic checks that run after someone sends new work to the server. This idea runs those checks as a chain of timers inside a small program on Cloudflare. Each timer runs one stage and then sets the next timer, with no separate queue service. The result is stored in the repo itself, so any git client can read it. Verdict: lands with caveats. [Read more](../plain/alarm-chain-ci.md).

**#15 Live fetch over hibernating WebSockets.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. A ref is a name that points at one commit. A branch is a ref. A tag is a ref. Verdict: lands with caveats. [Read more](../plain/live-fetch-websocket.md).

**#16 Branch-level Durable Objects for monorepos.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. Verdict: risky. [Read more](../plain/branch-level-dos.md).

**#17 Server-side three-way merge in the Worker.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. Verdict: risky. [Read more](../plain/server-side-merge.md).

**#18 Server-side rebase and squash as protocol v2 extensions.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. Verdict: risky. [Read more](../plain/server-side-rebase.md).

**#19 Signed refs by default with append-only DO reflog.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. A ref is a name that points at one commit. A branch is a ref. A tag is a ref. Verdict: lands with caveats. [Read more](../plain/signed-reflog.md).

**#20 Time-travel refs.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. Verdict: lands with caveats. [Read more](../plain/time-travel-refs.md).

**#21 Snapshots via R2 object versioning of ref state.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. A ref is a name that points at one commit. A branch is a ref. A tag is a ref. Verdict: lands with caveats. [Read more](../plain/r2-versioned-snapshots.md).

**#22 Copy-on-write forks.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A fork is a second repo that starts as a copy of a first repo, so a second person can change the copy. This idea makes a fork without copying any file content. The fork keeps a small list that says where each stored item lives, and stores only new items in the fork's own space. Verdict: risky. [Read more](../plain/cow-forks.md).

**#23 Pre/post-receive hooks as Workers via service bindings.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A push is sending your new commits to the server. A hook is a small program that the server runs when a push arrives. A pre-receive hook runs before the server accepts the push, and can reject the push. A post-receive hook runs after the push, and tells other systems what changed. Cloudflare Workers, or Workers, are small programs that run on Cloudflare's network close to the user, with no server to manage. This idea lets users write hooks as Workers and register them with the repo. Verdict: risky. [Read more](../plain/hooks-as-workers.md).

**#24 Per-blob presigned direct upload for giant pushes.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A push is sending your new commits to the server. An object is one stored item in git. An object is a file's content, a folder listing, or a commit. A blob is an object that holds one file's content. Big files make a push slow, because every byte passes through the server. This idea lets a special client send each big blob straight to the file store with a one-time signed link. The client then sends the server a small list of what was uploaded. A normal git push cannot use this idea, so the client must be a custom program. Verdict: risky. [Read more](../plain/presigned-direct-upload.md).

**#25 Wasm git core (gitoxide/libgit2) for delta resolution and merge.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. Cloudflare Workers, or Workers, are small programs that run on Cloudflare's network close to the user, with no server to manage. Wasm, or WebAssembly, is a way to run code from other languages, such as Rust, inside a Worker. Some git work is heavy, such as rebuilding a file from a delta or merging two versions of a file. This idea compiles a small part of gitoxide, a git library written in Rust, to Wasm. The TypeScript code does all reading and writing. The Wasm code only turns bytes into other bytes. Verdict: risky. [Read more](../plain/wasm-git-core.md).

**#26 Search index built on push (D1 FTS / Vectorize).** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A push is sending your new commits to the server. This idea builds a word search index for a repo each time a push arrives. The push finishes first. Then a background task reads the changed files and adds their text to a search table. A user can then search the repo's files by word. Verdict: lands with caveats. [Read more](../plain/search-index-on-push.md).

**#27 Diff API served with R2 range reads.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A diff is a list of what changed between two versions of one file. Normally a server rebuilds both full files and compares them. An object is one stored item in git. An object is a file's content, a folder listing, or a commit. A delta is a stored object written as "the same as that other object, with these changes". This idea reads only the delta from storage. The delta already says which bytes were copied and which bytes were inserted. The server turns that into a change summary without rebuilding the new file. Verdict: risky. [Read more](../plain/diff-api-range-reads.md).

**#28 Rate-limited, token-scoped remote URLs.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A push is sending your new commits to the server. A remote URL is the web address a git client uses to reach a server. This idea puts a signed token inside the URL. The token allows pushes to one branch only, for one hour only, with a limit on the number of pushes and bytes. The server checks the token with no database lookup. A counter in the repo's Durable Object enforces the limits exactly. Verdict: lands with caveats. [Read more](../plain/scoped-token-remotes.md).

**#29 Push from a sibling workspace DO over RPC, no HTTP.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A push is sending your new commits to the server. A commit is one saved version of the files, with a note about what changed. Verdict: lands with caveats. [Read more](../plain/tui-rpc-push.md).

**#30 Ephemeral repos with a self-destruct alarm.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. This idea creates a repo that lives for a set time, such as one hour. When the time runs out, the repo deletes all of its stored files and its own records. After that, git reports that the repo does not exist. A new repo with the same name can be created later, and the new repo starts clean. Verdict: lands with caveats. [Read more](../plain/ephemeral-repos.md).

**#31 Multi-writer CRDT branches.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A commit is one saved version of the files, with a note about what changed. A branch is a named line of commits, like a bookmark that moves forward as you save. In normal git, only one writer at a time can move a branch, and a second writer must first fetch and merge. A fetch is getting commits from the server. A clone gets everything for the first time. Verdict: does not land. [Read more](../plain/crdt-branches.md).

**#32 Agent-native protocol v2 commands (search, explain-diff, suggest-merge).** Git is a tool that keeps every version of a set of files, and lets many people share those versions. Git talks to a server with protocol v2. Protocol v2 is the newer, cleaner set of messages git uses to talk to a server. This idea adds three commands to that set: search, explain-diff, and suggest-merge. A normal git client ignores the three new commands. Verdict: lands with caveats. [Read more](../plain/agent-native-commands.md).

**#33 Semantic diffs via tree-sitter in Wasm.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A diff is a list of what changed between two versions of a file. A normal diff compares the two versions line by line. This idea compares them function by function, for JavaScript and Ruby code. Verdict: risky. [Read more](../plain/semantic-diffs.md).

**#34 Commit-as-event-stream.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A commit is one saved version of the files, with a note about what changed. A push is sending your new commits to the server. In this idea, every push that brings new commits also records one event per commit. The record is written in the same database step that moves the branch. A branch is a named line of commits, like a bookmark that moves forward as you save. Verdict: lands with caveats. [Read more](../plain/commit-event-stream.md).

**#35 Speculative packs.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A commit is one saved version of the files, with a note about what changed. A fetch is getting commits from the server. A clone gets everything for the first time. Verdict: risky. [Read more](../plain/speculative-packs.md).

**#36 Federated remotes via DO-to-DO gossip.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. Most git setups have one central server that holds the main copy of a repo. This idea removes the central server. Instead, each repo keeps a list of peer repos. When a branch moves in one repo, that repo tells its peers, and the peers tell their peers. Verdict: risky. [Read more](../plain/federated-gossip.md).

**#37 Encrypted-at-rest with client-held keys.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. In this idea, the user's computer locks every file with a secret key before the file leaves the computer. The server stores only locked files. The server never holds the key, so the server can never read the files. The server only knows the names of things and how they link together. Verdict: risky. [Read more](../plain/client-key-encryption.md).

**#38 Deduplication across all repos in one content-addressed bucket.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. Many repos hold the same files, such as a popular code library that thousands of projects copy. This idea stores each distinct file once for the whole service, no matter how many repos use it. Each repo keeps only a small note that says "I use this file". Verdict: lands with caveats. [Read more](../plain/global-dedup.md).

**#39 Merkle inclusion proofs on fetch.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. Sometimes a user wants one file from a repo without downloading the whole repo. The user also does not want to trust the server. This idea lets the server send the file plus a short chain of evidence. The user checks the chain with math and knows the file is the real one. Verdict: lands with caveats. [Read more](../plain/merkle-proofs.md).

**#40 Zero-clone execution: repo as a virtual filesystem inside an agent DO.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. Normally a program must download a whole repo before it can read one file. This idea skips that download. An automated agent reads each file straight from storage, by its fingerprint, only when the agent needs the file. The agent can also edit files and save a new version without ever holding the whole repo. Verdict: risky. [Read more](../plain/zero-clone-vfs.md).

**#41 Branch previews deployed as Workers on push.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A branch is a named line of commits, like a bookmark that moves forward as you save. A commit is one saved version of the files, with a note about what changed. In this idea, a user pushes a branch whose name starts with "preview". The server then puts that branch's files online as a small program with its own web address. Each preview branch gets its own live site within seconds. Verdict: lands with caveats. [Read more](../plain/branch-preview-workers.md).

**#42 Git as a database driver (ActiveRecord / JS ORM adapter).** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository, or repo, is one project's full set of files and their history. A commit is one saved version of the files, with a note about what changed. This idea uses a git repo as the storage behind an ordinary application database. Every time the application saves a record, the server writes one new commit. Every query reads the current records. A change to the shape of the records is a rewrite of every record on a side branch. Verdict: risky. [Read more](../plain/git-as-db-driver.md).

**#43 Bisect on the server with parallel test Workers.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A commit is one saved version of the files, with a note about what changed. Sometimes a bug appears, and nobody knows which commit caused the bug. Bisect is a search that tests commits in the middle of the history until it finds the first bad commit. This idea moves that search to the server and tests several commits at the same time. Verdict: lands with caveats. [Read more](../plain/server-side-bisect.md).

**#44 Ref leases.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A branch is a named line of commits, like a bookmark that moves forward as you save. A commit is one saved version of the files, with a note about what changed. A lease lets one person lock a branch for a set number of minutes. While the lease lasts, the server rejects a push to that branch from anyone else. A push is sending your new commits to the server. Verdict: lands with caveats. [Read more](../plain/ref-leases.md).

**#45 Commit graph in Vectorize for semantic git log.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A commit is one saved version of the files, with a note about what changed. The normal git log command lists commits in time order, and you can only search the notes for exact words. This idea lets you search commits by meaning. You type "the change that fixed the login timeout" and the server returns the commits that match best. Verdict: lands with caveats. [Read more](../plain/vectorized-commit-graph.md).

**#46 Time-boxed history with cold-storage checkpoints.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A commit is one saved version of the files, with a note about what changed. Old projects carry years of commits, and a new clone must download all of them. A fetch is getting commits from the server. A clone gets everything for the first time. The idea as stated was to squash commits older than one year into a few checkpoints. That is impossible, because every commit carries the fingerprint of its parent. Instead, the server hides history older than one year from a fresh clone and keeps every old object in cheaper storage. Anyone who wants the old history can still ask for it. Verdict: lands with caveats. [Read more](../plain/time-boxed-history.md).

**#47 Pull-request review data as git objects under refs/reviews.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository is one project's full set of files and their history. When people review each other's changes, the review comments normally live in a website database, not in git. If the website goes away, the comments go away. This idea stores every review comment as normal git data inside the repository. A clone then carries its own review history. A fetch is getting commits from the server. A clone gets everything for the first time. Verdict: lands with caveats. [Read more](../plain/reviews-as-refs.md).

**#48 Cross-repo atomic pushes.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository is one project's full set of files and their history. A push is sending your new commits to the server. A commit is one saved version of the files, with a note about what changed. Some changes must land in three repositories together, or not at all. This idea sends three pushes in one request. The server makes sure all three land, or none of them do. Verdict: risky. [Read more](../plain/cross-repo-atomic-push.md).

**#49 GitHub-compatible webhook payloads.** Git is a tool that keeps every version of a set of files, and lets many people share those versions. A push is sending your new commits to the server. A commit is one saved version of the files, with a note about what changed. A webhook is a message that a server sends to another web address when something happens. GitHub sends a push message in a well-known shape, and many tools already accept that shape. This idea makes git-edge send push messages in the GitHub shape, so those tools work without change. Verdict: lands with caveats. [Read more](../plain/github-webhook-compat.md).

**#50 Storage tiering by heat.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository is one project's full set of files and their history. This idea sorts the stored pieces of a repository by how often people read them. Pieces that people read often stay in fast storage that costs more. Pieces that nobody has read for a long time move to slow storage that costs less. Verdict: lands with caveats. [Read more](../plain/storage-tiering.md).

**#51 Blame that knows which agent wrote each line.** git is a tool that keeps every version of a set of files, and lets many people share those versions. git can already tell you which person last changed each line of a file. That report is called blame. This idea extends blame to AI agents. Each agent adds a short note to its commits with its session ID and model name. The server can then say which agent session wrote each line. Verdict: risky. [Read more](../plain/agent-blame.md).

**#52 Offline-first browser client with OPFS and the same Wasm core.** git is a tool that keeps every version of a set of files, and lets many people share those versions. This idea turns a web page into a git client. The page runs the same git code as the server, and stores files in the browser's own private file store, called OPFS. You can save your work with no network. When the network returns, the page sends your new work to the server. Verdict: risky. [Read more](../plain/offline-browser-client.md).

**#53 The /info/refs?service= entrypoint and pkt-line codec.** git is a tool that keeps every version of a set of files, and lets many people share those versions. Every talk between git and a server starts with one web request, the handshake. The server answers with a list of its refs and the features it supports. This idea builds that first request in a Worker, plus the small code that frames every git message. Verdict: lands. [Read more](../plain/info-refs-endpoint.md).

**#54 Auth and multi-tenancy: owner/repo routing to DO ids.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository is one project's full set of files and their history. Many people and many repositories share one service. This idea decides who is allowed to read or write each repository, and keeps each repository's data apart from all the others. A signed token proves who you are, and a small table inside the repository's own storage says what you are allowed to do. Verdict: lands with caveats. [Read more](../plain/auth-and-multitenancy.md).

**#55 GC and repack as a DO alarm.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A repository is one project's full set of files and their history. Over time a repository collects stored items that nothing points to anymore, such as the leftovers of a rejected upload. A repository also collects thousands of small items that would be faster to serve as one bundle. This idea runs a cleanup in the background, with no server to manage. The cleanup finds every item still in use, writes those items into one bundle, and deletes the rest. Verdict: risky. [Read more](../plain/gc-and-repack-alarm.md).

**#56 Want/have negotiation with a commit-graph in SQLite.** git is a tool that keeps every version of a set of files, and lets many people share those versions. A commit is one saved version of the files, with a note about what changed. When you ask a server for new commits, the server must work out which commits you lack. git does that with a short exchange, where you name the commits you want and the commits you already have. This idea answers that question from a small table of commits and their parents inside the server's own database. The server never has to read each commit from the large file store. Verdict: lands with caveats. [Read more](../plain/want-have-negotiation.md).

