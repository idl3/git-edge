# Interactive map

Open `index.html` in a browser, or serve this folder with any static server. `build.mjs` regenerates `git-edge.html` from `../data/results.json` and `template.html`; run it from the `site` folder after copying `results.json` and `themes.json` next to it as `data-full.json` and `themes.json`.
